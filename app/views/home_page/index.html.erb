<h1 style="text-align: center">
    Blog Mesmo
</h1>

<%= link_to 'New Post', new_post_path %>

<% @posts.each do |post| %>
  <p><%= post.title %></p>
  <p><%= post.text %></p>
  <p><%= link_to 'Show', post_path(post), controller: 'posts' %></p>
<% end %>